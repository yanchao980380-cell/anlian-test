# 项目评测约定

用户已认可七宗罪页面的新版框架与流程。后续新增评测，默认阅读并使用 `templates/assessment-v1/README.md`，从冻结底版创建独立页面，不直接复制仍在变化的 `sins/`。

- 用 `scripts/create-assessment.cjs` 初始化，不覆盖任何已有评测。
- 保留品牌导航、主题首页、密码弹窗、单题作答、进度与回看、分析过渡、明确结论、雷达、深入解读、行动建议、历史记录与文字下载流程。
- 每个评测首页不放合集入口，不展示题目数量；答题进度可以展示数量。
- 保留已认可的页面结构，主题配色、元素、文案随新主题调整；不要直接把页面改回旧版模板。
- 新评测必须重新设计题库、维度、评分与报告，不能只换标题。单/双模式和维度数按主题需要决定；当前七宗罪的每模式 35 题并非通用要求。
- 历史存储键按评测与评分版本隔离。新密码以本次用户要求为准，不继承其他评测密码。
- 主结果明确但不伪造分数；并列最高时，说明同分事实与代表项选择依据，保证雷达和结论一致。
- 定位为娱乐和自我探索，不写医学诊断、专业测量或无依据准确率，不承诺平台审核通过。
- 新页面接入根目录合集前，完成主题内容替换与模板验收。不要擅自修改其他评测或发布上线。

项目是普通静态 HTML/CSS/JS，无需迁移前端技术栈。模板是仓库内底版，不是 Codex 模板库中的个人技能。
